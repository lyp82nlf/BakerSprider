[project]
name = "barker-spider"
version = "0.2.0"
description = "Monitor Barker CEX campaigns and send WeCom notifications."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
